[pytest]
# Tests import the application as `app.*`. Without this, pytest puts the
# tests/ directory on sys.path instead of the project root and the import
# fails. Keep this file next to the code it configures.
pythonpath = .
testpaths = tests
